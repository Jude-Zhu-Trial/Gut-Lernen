/* Placeholder so local builds succeed before CI generates the real index.
 * firmware/tools/gen_audio.py (run in CI) overwrites this file. */

#include "gl_audio.h"

const gl_audio_entry_t GL_AUDIO_INDEX[] = {};
const uint32_t GL_AUDIO_COUNT = 0;
